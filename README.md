# arch-lxde-keyboard-layout
A small script that sets your keyboard layout for LXDE on Arch Linux using setxkbmap. It applies the layout immediately and also writes an @setxkbmap &lt;layout> entry into LXDE’s autostart so the change persists across logouts/reboots.
